# =================================================================
# الاعتماديات والمكتبات الخارجية (Dependencies & Vendor Libraries)
# =================================================================
# تجاهل المجلد الذي يحتوي على مكتبات PHP التي يتم تثبيتها عبر Composer
/vendor/
# تجاهل المكتبات الخارجية الخاصة بالقالب
/app-assets/vendors/

# =================================================================
# ملفات القالب الجاهز (CSS, JS, Assets)
# =================================================================
# تجاهل مجلدات التصميم الرئيسية للقالب
/app-assets/css/
/app-assets/css-rtl/

# تجاهل ملفات JavaScript الخاصة بالقالب.
# *ملاحظة: إذا قمت بتعديل أي ملف داخل هذا المجلد، أرسله بشكل منفصل.
/app-assets/js/

# الأصول الأخرى مثل الصور والخطوط
/app-assets/images/
/app-assets/fonts/
/assets/images/
/assets/fonts/

# =================================================================
# الملفات المرفوعة من قبل المستخدم
# =================================================================
# يجب تجاهل الملفات التي يقوم المستخدمون برفعها
/api/uploads/
/web-api/uploads/
/uploads/

# =================================================================
# الملفات المنشأة تلقائيًا (Build & Minified Files)
# =================================================================
# هذا يتجاهل أي ملف ينتهي بـ .min.css أو .min.js في أي مكان بالمشروع
*.min.css
*.min.js
*.css.map
*.js.map

# =================================================================
# ملفات الإعدادات والبيانات الحساسة (مهم جدًا للأمان)
# =================================================================
# هذه الملفات تحتوي على معلومات حساسة مثل بيانات الاتصال بقاعدة البيانات
# ومفاتيح التشفير، ويجب ألا يتم رفعها إلى مستودع عام.
/api/config/DB.php
/web-api/config/DB.php
/config/DB.php
/api/config/encryption.core.php
/web-api/config/encryption.core.php
/config/encryption.core.php
/api/config/settings.php
/web-api/config/settings.php
/config/settings.php

# =================================================================
# ملفات خاصة بالبيئة والخادم وقاعدة البيانات
# =================================================================
# تجاهل ملفات الإعدادات الخاصة بالخادم وملفات تفريغ قاعدة البيانات
/.user.ini
/php.ini
/the-doctor (database).sql

# Environment variables file
/.env

# Composer dependencies
/vendor/